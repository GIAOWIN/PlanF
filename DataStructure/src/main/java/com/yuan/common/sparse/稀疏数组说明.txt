稀疏数组
意义
    避免了内存的浪费，假设一个大容量int二维数组中，只有一小部分是有效数据，其他数据都是0，那么这种情况就是对内存的浪费。
实现原理
    1.创建一个int类型稀疏数组 new space[目标二维数组有效数字的容量+1][3]
    2.因为需要有一行来记录目标数组的参数 行数，列数，有效数字。
    3.遍历目标数组，将非0的元素添加到稀疏数组中，并记录此时该元素所在目标数组中的坐标
    4.结束，这样稀疏数组就创建好了。

